using System;

using UIKit;
using Foundation;
using ObjCRuntime;
using CoreGraphics;

namespace Rhino3dmIO.iOS
{
  // Rhino3dmIO.iOS is a binding (btouch) project.
  // Even though we are not binding Objective-C - but rather
  // wrapping a native C/C++ library (opennurbs) and packaging 
  // the Rhino3dmIO classes along with it - an ApiDefinition.cs is 
  // required for all btouch projects.
}
